{
  "name": "dominia",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "$npm_execpath run build && vite preview",
    "deploy": "$npm_execpath run build && wrangler deploy",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "hono:env": "tsx src/scripts/generate-dev-vars.ts",
    "test": "vitest run",
    "test:prepare": "tsx test/prepare.ts"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.2",
    "dotenv": "^17.2.3",
    "drizzle-orm": "^0.45.1",
    "hono": "^4.11.1",
    "postgres": "^3.4.7",
    "ws": "^8.18.3",
    "zod": "^4.2.1"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.10",
    "@cloudflare/vite-plugin": "^1.19.0",
    "@cloudflare/vitest-pool-workers": "^0.11.1",
    "@types/ws": "^8.18.1",
    "dotenv-expand": "^12.0.3",
    "drizzle-kit": "^0.31.8",
    "jose": "^6.1.3",
    "tsx": "^4.21.0",
    "vite": "^7.3.0",
    "vite-ssr-components": "^0.5.2",
    "vitest": "^3.2.4",
    "wrangler": "^4.56.0"
  }
}
