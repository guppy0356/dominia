# Copy this file to .env and adjust values as needed
# Run `npm run env:generate` after editing to generate .dev.vars for Cloudflare Workers

# Docker Compose environment variables (not included in .dev.vars)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=main
POSTGRES_HOST=dev.localtest.me

# Used for test preparation (not included in .dev.vars)
POSTGRES_URL=postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:5432

# Runtime variables (written to .dev.vars - used by Cloudflare Workers)
DATABASE_URL=${POSTGRES_URL}/${POSTGRES_DB}

# Optional: JWKS URI for authentication (written to .dev.vars)
# JWKS_URI=https://your-auth-provider.com/.well-known/jwks.json
