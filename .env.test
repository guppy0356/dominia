# Test environment configuration
# This file is committed to the repository for consistent test environments

# Docker Compose environment variables
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=test
POSTGRES_HOST=test.localtest.me

# Database connection URL
POSTGRES_URL=postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:5432

# Runtime variables (written to .dev.vars.test - used by Cloudflare Workers)
DATABASE_URL=${POSTGRES_URL}/${POSTGRES_DB}
